import { SimpleForm, CustomForm } from "./gui";


export declare class mc {
    // Factory method for creating instances of GUI.
    static newSimpleForm(): SimpleForm;
    static newCustomForm(): CustomForm;
}
